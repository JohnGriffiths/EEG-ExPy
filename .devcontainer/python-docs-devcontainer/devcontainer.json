{
    "name": "eegnb-docs",
    "image": "mcr.microsoft.com/devcontainers/python:3.8",
      
    "customizations": {
      "vscode": {
        "extensions": [
          "ms-python.python"
        ],
        "settings": {
          "python.pythonPath": "/usr/local/bin/python"
        }
      }
    },
  
    "forwardPorts": [
      8000
    ],

    "postCreateCommand": "make install-deps-apt && python -m pip install --upgrade pip wheel && python -m pip install attrdict && make install-deps-wxpython && make build && make docs && pip install -r requirements.txt && pip install -e . && echo 'Dependencies installed' && cd doc && mkdir _static && echo 'Created _static directory' && make html && echo 'HTML documentation built'",
    "appPort": 8000
  
  }

